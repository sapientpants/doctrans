#!/bin/sh
set -e

# Applies pending migrations and exits. A release carries no Mix, so this goes
# through `eval`, which starts the repository without starting the endpoint.
cd -P -- "$(dirname -- "$0")"
exec ./doctrans eval "Doctrans.Release.migrate()"

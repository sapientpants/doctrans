# Database configuration
DATABASE_HOST=db
DATABASE_URL=ecto://postgres:postgres@db/doctrans_dev

# Ollama configuration (points to host machine's Ollama)
# Use http://host.docker.internal:11434 for Docker on macOS/Windows
# Use http://172.17.0.1:11434 for Docker on Linux (or use host-gateway)
OLLAMA_HOST=http://host.docker.internal:11434

# Phoenix configuration
PHX_HOST=localhost
PORT=4000
